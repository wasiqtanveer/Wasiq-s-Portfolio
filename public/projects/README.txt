Project preview images live here. Current files:

  ads acceleration.jpg   -> Ads Acceleration
  Paper Vault.jpg        -> Paper Vault
  CR attendance app.jpg  -> CR Attendance App
  (Library Management System has no image yet -> shows initials only)

The filenames are referenced (URL-encoded for spaces) in:
  src/components/Projects.jsx
  src/pages/WorkArchive.jsx

If you rename a file, update the matching `image:` path in those two files.
Recommended: ~16:10 ratio, around 1200x750px.
